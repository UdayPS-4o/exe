services:
  app:
    build: .
    volumes:
      - app-data:/app/data
    restart: unless-stopped
    environment:
      - PORT=8080
    networks:
      - dokploy-network

volumes:
  app-data:

networks:
  dokploy-network:
    external: true
