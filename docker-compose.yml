version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - app-data:/app/data
    restart: unless-stopped
    environment:
      - PORT=8080

volumes:
  app-data:
